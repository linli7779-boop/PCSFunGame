{
  "name": "pcs-chinese-learning-fun-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/buildData.mjs && vite",
    "build": "node scripts/buildData.mjs && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.3"
  }
}
